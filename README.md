# Docker
1- SettingUp PostgreSQL n pgAdmin

2- PostgreSQL skills- like JOIN, GROUP BY, and ORDER BY.

3- Database Structure
 -Tables:

a) Players

b)  Games

c) Scores

4-  1. Initialize Your Project
o Create a new folder for this project and run npm init -y

o Install the necessary dependencies:
npm install express pg dotenv nodemon

   2- Set Up the Database
   
   3- Create a Basic Express Server ( Set up an Express app in index.js and configure it to connect to your PostgreSQL database.)
# Docker
